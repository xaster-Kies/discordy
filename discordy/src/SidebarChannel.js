import React from 'react'
import './SidebarChannel.css'

function SidebarChannel({id, channel}) {
    return (
        <div className="sideBarChannel">
            <h4><span className="sideBarChannel__hash">#</span>JavaScript</h4>
        </div>
    )
}

export default SidebarChannel
